{{- /*
V133 (2026-06-21): Worker PDB is skipped in DR standby mode because
the Worker Deployment itself is skipped (scaled to 0). Creating a PDB
for a non-existent Deployment is invalid and wastes API server quota.
*/ -}}
{{- $standbyMode := and .Values.multiRegion.enabled .Values.multiRegion.secondary.standby }}
{{- if .Values.podDisruptionBudgets.api.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "fireai.fullname" . }}-api-pdb
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "fireai.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  minAvailable: {{ .Values.podDisruptionBudgets.api.minAvailable }}
  selector:
    matchLabels:
      {{- include "fireai.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
---
{{- end }}
{{- if and .Values.podDisruptionBudgets.worker.enabled (not $standbyMode) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "fireai.fullname" . }}-worker-pdb
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "fireai.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  minAvailable: {{ .Values.podDisruptionBudgets.worker.minAvailable }}
  selector:
    matchLabels:
      {{- include "fireai.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
{{- end }}
