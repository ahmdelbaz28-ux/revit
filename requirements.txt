# Requirements for CAD/BIM integration system
#
# V133 (2026-06-22): Synced with pyproject.toml [project.dependencies].
# Previous requirements.txt was missing 17 critical dependencies that the
# application imports at runtime — the Docker image would crash on startup.
# All versions are pinned for reproducible builds and Python 3.11 compat.

# ── Core web framework ──────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings>=2.0.0,<3.0.0
python-multipart==0.0.6
slowapi==0.1.9
packaging>=23.0

# ── Database ────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.10.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0
asyncpg>=0.27.0,<1.0.0

# ── Message queue / cache ───────────────────────────────────────────────
redis>=4.5.0,<5.0.0
celery>=5.2.0,<6.0.0

# ── Authentication / security ───────────────────────────────────────────
pyjwt[crypto]>=2.6.0,<3.0.0
passlib[bcrypt]>=1.7.0,<2.0.0
cryptography>=41.0.0,<43.0.0

# ── Async / networking ──────────────────────────────────────────────────
websockets>=10.0.0,<12.0.0
tenacity>=8.2.0,<9.0.0
requests>=2.28.0,<3.0.0

# ── Utilities ───────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0
python-dateutil>=2.8.0,<3.0.0
pytz>=2023.0.0,<2025.0.0
psutil==5.9.6

# ── Scientific computing ────────────────────────────────────────────────
numpy==1.24.3
pandas==2.1.3
matplotlib==3.8.2
scipy==1.11.4

# ── Parsing ─────────────────────────────────────────────────────────────
lxml==4.9.3
beautifulsoup4==4.12.2

# ── Testing (needed for CI Docker image import check) ───────────────────
hypothesis>=6.82.0,<7.0.0
