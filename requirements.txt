# Auto-generated from pyproject.toml — unified for Python 3.12+
# SECURITY: Pinned minimum versions to resolve known vulnerabilities
psutil>=5.8.0
shapely>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn>=0.20.0
sqlalchemy>=2.0.0
pyyaml>=6.0
requests>=2.32.0
urllib3>=2.7.0      # CVE-2026-26812: insufficient header validation in <2.7.0
python-multipart>=0.0.9    # PYSEC-2024-60: path traversal in <0.0.7
pyjwt>=2.6.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
python-dotenv>=1.0.0
alembic>=1.10.0

# Extra critical deps for API services
httpx>=0.27.0
cryptography>=42.0.0     # CVE-2024-0727: timing attack in <42.0.0
argon2-cffi>=23.0.0
websockets>=12.0
tenacity>=8.2.0

# CAD and data processing
ezdxf>=1.0.0
ifcopenshell>=0.7.0
opencv-python>=4.5.0
pymupdf>=1.23.0
pillow>=10.0.0      # CVE-2023-44271: DOS in <10.0.0
pandas>=1.3.0
networkx>=2.5
sympy>=1.8

# Async (optional — see requirements-optional.txt for FACP distributed deps)
# redis>=4.0.0       → moved to requirements-optional.txt
# celery>=5.1.0      → moved to requirements-optional.txt
# asyncio-mqtt>=0.10.0 → moved to requirements-optional.txt

# Workflow engine (V129 FIX: langgraph now required for workflow service)
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=0.1.0

# Testing (dev)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
# locust>=2.20.0    → moved to requirements-optional.txt

# Code quality (V129 FIX: dev tools added for linting/type checking)
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0
wheel>=0.46.2       # CVE-2026-24049: path traversal in <0.46.2

# Report generation
reportlab>=4.0.0

# Property-based testing
hypothesis>=6.0.0
