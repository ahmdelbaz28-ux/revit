# FireAI ML Subsystem — Additional Requirements
# ===============================================
# These packages extend the existing requirements.txt with ML libraries
# curated from https://github.com/josephmisiti/awesome-machine-learning
#
# Install with: pip install -r requirements-ml.txt
# (Existing requirements.txt stays unchanged for backward compatibility)

# ── Gradient Boosting (Predictive Maintenance) ──────────────────────────────
# Source: awesome-machine-learning/README.md → Python → General-Purpose ML
xgboost>=2.0.0          # Industry-standard gradient boosting (SHAP-native)
lightgbm>=4.0.0         # Faster alternative to XGBoost for large datasets

# ── General ML Primitives ───────────────────────────────────────────────────
# Source: awesome-machine-learning → Python → General-Purpose ML
scikit-learn>=1.3.0     # Train/test split, metrics, preprocessing

# ── Explainability (Safety-Critical Requirement) ───────────────────────────
# Source: awesome-machine-learning → Python → General-Purpose ML
shap>=0.43.0            # SHapley Additive exPlanations (mandatory for audit)
lime>=0.2.0             # Local Interpretable Model-agnostic Explanations

# ── Time-Series Forecasting ─────────────────────────────────────────────────
# Source: awesome-machine-learning → Python → General-Purpose ML
prophet>=1.1.4          # Facebook Prophet for seasonal failure patterns
statsmodels>=0.14.0     # ARIMA, Holt-Winters (complements existing analytics)

# ── Survival Analysis (Censored Time-to-Event) ─────────────────────────────
# Source: awesome-machine-learning → Python → Survival Analysis section
lifelines>=0.27.0       # Cox PH, Kaplan-Meier, Weibull fitters

# ── Deep Learning (Optional — for LSTM) ────────────────────────────────────
# Source: awesome-machine-learning → Python → General-Purpose ML
# Note: PyTorch is a heavy dependency; install only if LSTM models needed
# torch>=2.0.0           # Uncomment to enable LSTM time-series model

# ── Experiment Tracking (Optional — for production MLOps) ───────────────────
# Source: awesome-machine-learning → Tools → Misc
# mlflow>=2.7.0          # Uncomment for production model registry
# evidently>=0.4.0       # Uncomment for model drift detection
